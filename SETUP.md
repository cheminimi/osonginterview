# 면접 스튜디오 v4 — 설치·운영 가이드

**구글 시트(학생별 준비 시트·운영 원본)는 그대로 쓰고**, 시트가 못 하는 일을 플랫폼이 맡습니다.

| 구글 시트 (기존) | 면접 스튜디오 (플랫폼) |
|---|---|
| 학생 자기분석·생기부 분석·교과 선생님 질문 답변 | 학생: D-day·지원 대학 확인, 준비 시트 바로가기, **타이머 말하기 연습**(질문 읽어주기·받아쓰기·꼬리질문), 선생님 피드백 보기 |
| 수요조사·운영 원본·배정안 (**면접일·배정은 '앱연동' 탭과 쌍방 동기화**) | 학생·교사: **모의면접 일정 달력** (신청·수락·시간 변경, 교사·장소 겹침 차단) → 시트 '면접 기록'·'면접 일정표' |
| | 교사: **내 담당 학생 대시보드**, 학생 상세(배정·대학·수요조사), **대면 모의면접 기록 → 통합 플랫폼 시트 자동 기록**, 예상질문·질문은행, 연습 리뷰 |

## 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 로그인 (학생: 학번 / 선생님: 교사 ID, 최고 관리자는 이메일) |
| `student.html`, `interview.html` | 학생 홈, 말하기 연습 |
| `teacher.html` + `t-*.js` | 선생님 화면 (대시보드·일정·학생·대면 기록·예상질문·질문은행·연습 리뷰·관리) |
| `schedule.js` | 모의면접 일정 달력 (학생·교사 화면 공용) |
| `common.js`, `sync.js`, `importers.js`, `prompts.js`, `seed-questions.js` | 공통 기능, 시트 동기화, 시트 표 읽기, Claude 프롬프트, 기본 질문 34개 |
| `firebase-config.js` | **Firebase 설정값 · 최고 관리자 이메일 (직접 수정)** |
| `firestore.rules` | **보안 규칙 (관리자 이메일 직접 수정)** |
| `apps-script/Code.gs` | **수요조사 스프레드시트에 붙일 쌍방 동기화 스크립트** (GitHub에 올리지 않아도 됨) |

---

## 1. Firebase 준비 (처음 한 번, PC에서)

1. [Firebase 콘솔](https://console.firebase.google.com) → 새 프로젝트 (기존 탐구 플랫폼과 **분리**)
2. **Authentication → 로그인 방법 → 이메일/비밀번호** 사용 설정
3. **Authentication → 사용자 추가**: 최고 관리자 계정 (본인 Gmail + 비밀번호)
4. **Firestore Database → 만들기** (위치 `asia-northeast3` 서울, 프로덕션 모드)
5. **프로젝트 설정 → 내 앱 → 웹 앱 추가** → `firebaseConfig` 값 복사
6. `firebase-config.js`의 `firebaseConfig`에 붙여넣기, `ADMIN_EMAILS` 확인
7. **Firestore → 규칙** 탭에 `firestore.rules` 전체 붙여넣기 → 게시 (`adminEmails()`가 6번과 같은지 확인)

## 2. GitHub Pages 배포

1. GitHub 새 저장소 → 파일 전체 업로드 (`index.html`이 최상위)
2. **Settings → Pages → Branch: main / (root)** → Save → `https://아이디.github.io/저장소/`
3. ⚠ **Firebase → Authentication → 설정 → 승인된 도메인**에 `아이디.github.io` 추가

## 3. 시트 쌍방 연동 (Apps Script, PC에서 한 번)

### 무엇이 연동되나
| 시트 | 앱 | 방향 |
|---|---|---|
| 수요조사 파일 **`앱연동_학생`** 탭 (이름·반·우선도·트랙·특별 트랙·1~3차 담당·준비 시트 링크) | 학생 정보 | ⇄ 쌍방 |
| 수요조사 파일 **`앱연동_면접일`** 탭 (학번·대학·학과·전형·면접 유형·**면접일**) | 학생별 지원 대학·D-day | ⇄ 쌍방 |
| 통합 플랫폼 파일 **`모의 면접 기록`** 탭 (학반·학번·이름·유형·담당교사·실시일·회차) | 대면 모의면접 기록 | ⇄ 쌍방 (피드백 내용은 앱에만) |
| 설문지 응답 탭 (희망 교과·준비 정도·도움 필요·전달 메모) | 학생 상세의 수요조사 칸 | → 시트에서 앱으로만 |
| 수요조사 파일 **`면접 기록`** 탭 (일정 신청·수락·변경·취소 한 줄씩) · **`면접 일정표`** 탭 (지금 잡힌 일정) | 모의면접 일정 | ← 앱에서 시트로만 (자동 생성) |
| 통합 플랫폼 **`모의 면접 기록`** 탭 | 일정이 **확정**되면 예정 행 자동 추가 | ← 앱에서 |

- **시트에서 고치면 수 초 안에** 앱에 반영 (편집 트리거), **30분마다(07~22시)** 전체를 다시 대조합니다.
- **앱에서 고치면 바로** 시트에 반영됩니다. 네트워크 문제로 실패해도 정기 대조 때 맞춰집니다.
- 같은 칸을 양쪽에서 거의 동시에 고치면 **나중에 고친 쪽**이 남습니다.
- 삭제: 면접일 행은 어느 쪽에서 지워도 양쪽에서 지워집니다 · 학생은 시트 행을 지워도 앱에서 지우지 않습니다 · 모의 면접 기록은 **앱에서만** 삭제 (시트에서 행을 지우면 다시 채워짐)
- 안전장치: 한쪽 데이터가 통째로 비어 보이면(연결 오류 등) 삭제하지 않고 멈춘 뒤 `_동기화기록` 숨김 탭에 오류를 남깁니다.

### 설치
1. 앱에 최고 관리자로 로그인 → **관리 → ① 시트 연동 → 동기화 계정 만들기** → 이메일·비밀번호 복사해 두기
2. 드라이브에서 **'2026. 대입 면접 대비 프로그램 수요조사'** 스프레드시트 열기 → **확장 프로그램 → Apps Script**
3. 기본 코드 지우고 `apps-script/Code.gs` 전체 붙여넣기 → 맨 위 `CONFIG`에 `firebase-config.js`의 **apiKey, projectId** 입력 → 저장
4. 스프레드시트 새로고침 → 위 메뉴에 **면접 스튜디오** 생김 → **① 연동 탭 만들기**
   - 배정안·설문지 응답·학생별 링크 파일에서 읽어 `앱연동_학생`(93명)·`앱연동_면접일`(대학별 210건 안팎) 탭을 만듭니다. 수요조사에 응답이 없는 학생은 배정안의 최초 면접일로 한 줄 만듭니다.
   - 처음 실행할 때 권한 허용 창이 뜹니다 (스프레드시트·외부 연결·드라이브)
5. Apps Script 화면 **배포 → 새 배포 → 톱니바퀴: 웹 앱** / 실행 계정 **나** / 액세스 권한 **모든 사용자** → 배포 → **웹 앱 URL**(`…/exec`) 복사
6. 스프레드시트 메뉴 **② 앱 연결 설정** → 1번의 동기화 계정 이메일·비밀번호 입력 → 표시되는 **토큰** 복사
7. 앱 **관리 → ① 시트 연동**에 웹 앱 URL·토큰 저장 → **연결 테스트**
8. 스프레드시트 메뉴 **③ 자동 동기화 켜기** → 첫 전체 동기화가 돌며 앱에 학생·면접일이 들어갑니다
9. (권장) 메뉴 **④ 기존 탭 날짜를 연동 탭과 연결**
   - 먼저 파일 전체 **백업 사본**을 드라이브에 만든 뒤, 배정안·운영 일정·운영 명단·편성표·담당교사 배정안의 *최초 면접일*과 배정안의 *트랙·1~3차 담당*, 운영 명단·교사별 담당 학생의 *지원 대학·학과·면접일* 칸을 연동 탭을 참조하는 수식으로 바꿉니다.
   - 이후 날짜·배정은 **`앱연동` 탭(또는 앱)에서만** 고치면 나머지 탭이 자동으로 따라 바뀝니다.
   - *준비 시작일·권장 시작일* 같은 준비 일정 칸, *교사별 담당 학생* 탭의 행 목록은 그대로입니다. 면접일이 크게 바뀌거나 담당이 바뀐 학생은 직접 확인하세요.
   - *5. 면접 달력* 탭은 면접일이 바뀔 때마다 자동으로 다시 그려집니다 (메뉴에서 수동 실행도 가능).

### 운영 팁
- 새 학생: `앱연동_학생`에 한 줄 추가 → 앱에 생김 → 관리 → ③ 학생 계정에서 발급
- 면접일이 바뀜: `앱연동_면접일`에서 날짜만 고치거나, 앱 학생 상세 → 지원 대학 · 면접일 → 수정
- 대학 추가: 시트에 행 추가(행ID 칸은 비워 두면 자동 생성) 또는 앱에서 **+ 대학 추가**
- `행ID`, `기록ID` 칸은 지우거나 고치지 마세요 (숨기기는 괜찮음). 행 정렬·필터는 자유롭게 해도 됩니다.
- 동기화 내역은 숨김 탭 `_동기화기록`에서 볼 수 있습니다.
- Apps Script 코드를 고친 뒤에는 **배포 관리 → 수정(연필) → 버전: 새 버전**으로 다시 배포하세요.

## 4. 계정 만들기 (관리 탭, PC에서)

1. **② 교사 계정**: *8. 지도교사 명단* 또는 *3회 지도 배정안* 위쪽 표(교사명 · 교과·역할)를 머리행과 함께 붙여넣기 → ID(기본 t01, t02…)·권한 확인 → 계정 만들기 → **안내문 복사**해서 전달
   - 이름이 배정표의 교사명과 같아야 '내 담당 학생'이 연결됩니다
   - 3학년부장 등은 '관리자'로 두면 관리 탭을 함께 쓸 수 있습니다
2. **③ 학생 계정**: 미발급 학생 발급 → **표 복사**(학번·이름·초기 비밀번호·안내문) → 시트에 붙여 개별 발송
3. **질문은행 탭 → 기본 질문 불러오기** (인성·MMI·제시문 연습용)

> Firebase는 같은 인터넷 주소에서 **짧은 시간에 계정을 너무 많이 만들면 잠시 막습니다** (대략 시간당 100개 안팎). 학생+교사가 100명이 넘으므로, 발급이 중간에 멈추면 1시간쯤 뒤 같은 버튼을 다시 누르세요 — 남은 사람만 이어서 발급합니다.

## 5. 운영

### 선생님
- **대시보드**: 내가 1·2·3차로 배정된 학생, D-day 순. 행의 **기록** 버튼 → 대면 모의면접 기록 (차수·담당교사·면접 유형이 배정표 기준으로 자동 선택)
- **학생**: 전체 조회 (반·트랙·특별 트랙·내 담당 필터). 학생을 누르면 배정·지원 대학·수요조사 메모·대면 기록·연습 기록·**교사 메모**(학생에게 안 보임). **면접일·트랙·배정 수정**도 여기서 (시트에 바로 반영). 다른 선생님이 시트에서 고친 내용은 앱을 새로고침하면 보입니다.
- **예상질문**: 학생 준비 시트의 *교과 선생님 질문* 표를 복사해 붙이면 그대로 말하기 연습 질문이 됩니다 (예시 행 자동 제외). 생기부 → Claude 프롬프트 방식도 가능
- **연습 리뷰**: 학생이 제출한 말하기 연습에 점수·코멘트·AI 피드백

### 학생
- 첫 로그인 때 비밀번호 변경 (8자 이상)
- 트랙에 맞는 연습이 **내 트랙 추천**으로 먼저 보입니다
- 대면 모의면접 피드백을 확인하고, 준비 시트의 피드백 기록 탭에 옮겨 적어 답변 수정

### 비밀번호
- 누구나 상단 **비밀번호** 버튼으로 변경
- 잊어버리면 관리자가 **비번 재발급** → 새 초기 비밀번호 (이전 기록은 그대로 연결)

## 6. 모의면접 일정 (1·2·3차)

### 흐름
1. **신청**: 학생은 *모의면접 일정* 탭 달력에서 칸의 **＋**를 눌러 차수·시간·장소를 골라 신청 (상대는 배정표의 1차 담임 / 2차 교과 / 3차 위원 2명). 선생님도 *일정* 탭에서 담당 학생에게 먼저 제안할 수 있습니다.
2. **응답**: 상대가 **수락 / 시간 변경 제안 / 거절**. 3차는 학생·위원 2명이 **모두 수락**해야 확정.
3. **확정**: 달력에 실선으로 표시, 시트 `모의 면접 기록`에 예정 행이 생김. 면접 후 일정 상세의 **대면 기록 쓰기**로 피드백 작성.
4. 확정 후에도 누구나 **시간 변경 제안·취소** 가능. 모든 행동은 시트 `면접 기록` 탭에 남습니다.

### 시간 칸
- 1~7교시·점심·방과후·야자 1·2차시. **1차는 칸 전체**, **2·3차는 칸 안에서 30분 단위** (방과후 3자리, 점심·야자 2자리, 교시 1자리)
- 시간표·장소 목록은 **관리 → ④ 일정 설정**에서 수정 (기본 장소: SW실, 진로진학실). 선생님은 목록에 없는 장소를 **기타**로 직접 입력

### 겹침 규칙
- 달력은 **모든 학생·교사가 함께 봅니다** (학생 화면에서 다른 학생 이름은 `박○○`처럼 가림)
- **같은 선생님** 또는 **같은 장소**가 시간이 겹치면 신청 불가. 선생님이 다르고 장소도 다르면 같은 시간에 여러 건 가능
- 같은 학생의 일정끼리도 겹칠 수 없음. **요청 중인 일정도 자리를 차지**합니다 (거절·취소하면 비워짐)
- 선생님이 **내 불가 시간**(요일별 수업 교시·날짜별 출장 등)을 체크하면 학생 달력에 회색으로 보이고 학생은 그 시간에 신청 불가 (선생님 본인·관리자는 경고만)
- 대학 면접일보다 늦은 날짜를 고르면 경고

## 7. 알아둘 점
- **음성 받아쓰기**는 크롬·엣지(PC·안드로이드)에서 잘 됩니다. 아이폰은 직접 입력으로 대체.
- 보안 규칙: 학생은 자기 정보·자기 기록·공개된 피드백만, 교사 메모·다른 학생·설정은 못 봅니다. 교사는 전체 학생 조회, 계정·설정은 관리자만.
- 졸업 후에는 Firestore 데이터와 Authentication 사용자를 삭제하세요.

---

## 이미 v3를 설치했다면: v4 업데이트 순서
1. **GitHub 저장소에 파일 올리기** (Add file → Upload files, 같은 이름은 덮어씀)
   - 새 파일: `schedule.js`, `t-schedule.js`
   - 바뀐 파일: `common.js`, `sync.js`, `student.html`, `teacher.html`, `style.css`, `t-admin.js`, `t-core.js`, `t-meetings.js`
   - `firebase-config.js`는 올리지 마세요 (내가 고친 설정값이 덮어써짐)
2. **Firebase → Firestore → 규칙**: 새 `firestore.rules` 전체 붙여넣기 → 게시 (맨 위 관리자 이메일 확인)
3. **Apps Script**: 코드 전체를 새 `Code.gs`로 바꾸기 (맨 위 CONFIG의 apiKey·projectId 다시 입력) → 저장 → **배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포** (URL은 그대로)
4. 앱 **관리 → ① 시트 연동**에서 **저장**을 한 번 더 누르기 (학생 화면이 일정 알림 주소를 읽을 수 있게 됨)
5. 앱 **관리 → ④ 일정 설정**에서 장소·시간표 확인 → 저장
6. 선생님들께 **일정 탭 → 내 불가 시간**에 수업 교시 체크 안내
